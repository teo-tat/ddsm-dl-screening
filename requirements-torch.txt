# PyTorch localiser members. A separate environment from .ddsm-env: torch and
# tensorflow[and-cuda] pin incompatible nvidia-* wheels and cannot share one.
# python3.11 -m venv .torch-env && .torch-env/bin/pip install -r requirements-torch.txt
torch==2.8.0
torchvision==0.23.0
segmentation-models-pytorch==0.4.0
timm==1.0.11
numpy==1.26.4
pandas==3.0.2
scipy==1.17.1
